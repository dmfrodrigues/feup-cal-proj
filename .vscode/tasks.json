{
    "tasks": [
        {
            "type": "shell",
            "label": "build-test",
            "command": "make",
            "args": [
                "./obj/test"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "build-empirical",
            "command": "make",
            "args": [
                "-C",
                "empirical"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}