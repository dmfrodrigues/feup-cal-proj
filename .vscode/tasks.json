{
    "tasks": [
        {
            "type": "shell",
            "label": "build-main",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "build-test",
            "command": "make",
            "args": [
                "./obj/test"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}