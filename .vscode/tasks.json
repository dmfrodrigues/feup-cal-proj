{
    "tasks": [
        {
            "type": "shell",
            "label": "build-test",
            "command": "make",
            "args": [
                "./obj/test"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}